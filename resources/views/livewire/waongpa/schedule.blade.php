<div><p class="eyebrow">ตารางส่วนตัว</p><h1>My Schedule</h1><p>บันทึกช่วงที่ไม่ว่าง ระบบจะคัดลอกไปใช้เมื่อคุณเข้าร่วมรอบนัด</p>@include('waongpa.feedback')
<form wire:submit="save" class="panel"><h2>{{ $editing?'แก้ไขกิจกรรม':'เพิ่มช่วงไม่ว่าง' }}</h2>@include('waongpa.event-fields')<div class="actions"><button wire:loading.attr="disabled">บันทึก</button>@if($editing)<button type="button" class="secondary" wire:click="clear">ยกเลิกการแก้ไข</button>@endif</div></form>
<section class="panel"><h2>กิจกรรมของฉัน</h2><div class="table-wrap"><table><thead><tr><th>กิจกรรม</th><th>เริ่ม</th><th>สิ้นสุด</th><th>จัดการ</th></tr></thead><tbody>@forelse($events as $event)<tr wire:key="event-{{ $event->id }}"><td>{{ $event->title }}</td><td>{{ $event->starts_at->format('d/m/Y H:i') }}</td><td>{{ $event->ends_at->format('d/m/Y H:i') }}</td><td><button class="secondary" wire:click="edit({{ $event->id }})">แก้ไข</button> <button class="danger" wire:confirm="ลบกิจกรรมนี้จาก My Schedule? สำเนาในรอบเดิมยังคงอยู่" wire:click="delete({{ $event->id }})">ลบ</button></td></tr>@empty<tr><td colspan="4">ยังไม่มีกิจกรรม เพิ่มช่วงไม่ว่าง หรือยืนยันว่าว่างในหน้ารอบนัด</td></tr>@endforelse</tbody></table></div></section></div>
