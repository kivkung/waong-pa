<div><div class="hero"><p class="eyebrow">พื้นที่นัดหมายของทุกคน</p><h1>หาเวลาที่ลงตัว<br>แล้วนัดกันอีกครั้ง</h1><p>ดูห้องสาธารณะ หรือเข้าสู่ระบบเพื่อเตรียมตารางและโหวตเวลานัด</p></div>
@include('waongpa.feedback')
<section class="panel"><div class="grid two"><label>ค้นหาห้อง<input wire:model.live.debounce.350ms="search" placeholder="ชื่อห้อง"></label>@auth<label class="check"><input type="checkbox" wire:model.live="mine"> เฉพาะห้องที่ฉันเข้าร่วม</label>@endauth</div>
@auth<form wire:submit="join" class="inline"><label>รหัสเข้าร่วมห้อง<input wire:model="code" maxlength="12" placeholder="รหัส 12 ตัวอักษร" required></label><button wire:loading.attr="disabled">เข้าร่วมห้อง</button></form>@endauth</section>
<div class="grid three">@forelse($rooms as $room)<article class="panel" wire:key="room-{{ $room->id }}"><span class="badge">{{ $room->visibility }}</span><h2>{{ $room->name }}</h2><p>{{ Str::limit($room->description,130) }}</p><a class="button secondary" href="{{ route('waongpa.rooms.show',['roomId'=>$room->id]) }}">ดูห้อง</a></article>@empty<p class="panel">ยังไม่มีห้องที่ตรงกับการค้นหา</p>@endforelse</div><div class="pagination">{{ $rooms->links() }}</div></div>
