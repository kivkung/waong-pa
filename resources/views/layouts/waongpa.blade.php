<!DOCTYPE html><html lang="th"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="csrf-token" content="{{ csrf_token() }}"><title>Waongpa · นัดเวลาที่ตรงกัน</title><link rel="stylesheet" href="{{ asset('waongpa.css') }}">@livewireStyles</head>
<body><header><a class="brand" href="{{ route('waongpa.index') }}">Waongpa <small>หาเวลาที่ตรงกัน</small></a><nav><a href="{{ route('waongpa.index') }}">ห้องนัดหมาย</a>@auth<a href="{{ route('waongpa.schedule') }}">My Schedule</a><a href="{{ route('waongpa.rooms.create') }}">สร้างห้อง</a><span>{{ auth()->user()->name }}</span><form method="POST" action="{{ route('logout') }}">@csrf<button class="secondary">ออกจากระบบ</button></form>@else<a href="{{ route('login') }}">เข้าสู่ระบบ</a><a class="button" href="{{ route('register') }}">สมัครสมาชิก</a>@endauth</nav></header><main>{{ $slot }}</main><footer>Waongpa · เวลาทั้งหมด {{ config('waongpa.timezone') }}</footer>@livewireScripts</body></html>
